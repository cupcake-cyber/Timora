package com.timora.app.model.enums;

public enum GlobalRole {
    OWNER,// Control total de la empresa/tenant
    ADMIN,// Administración organizacional general
    USER,// Usuario interno estándar sin autoridad organizacional
}